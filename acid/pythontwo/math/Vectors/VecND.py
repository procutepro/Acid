# any number of elements in a vector

class vectND:
    def __init__(self, n, elements):
        self.n = n
        self.elements = elements
    
    def returnvec(self):
        return str(tuple(self.elements))
    
    def add(self, other):
        for i in range(len(self.elements)):
            pass
    